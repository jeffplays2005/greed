/**
 * Cache collection keys for `bot.cache`
 */
export enum CacheCollectionKeys {
  COMMAND_FOLDERS = "command-folders",
  FOLDER_MAPPING = "folder-mappings",
  GLOBAL_DISABLED_COMMANDS = "global-disabled-commands",
}
